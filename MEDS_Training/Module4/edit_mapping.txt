# Edit mapping for Module4 PDF changes

You asked for small variable renames so PDFs aren't verbatim copies. Apply the following mapping consistently across all source files before regenerating PDFs.

Mapping (automated):
- addr -> addr_mod
- data -> data_mod
- en -> en_mod
- clk -> clk_mod
- rst -> rst_mod
- valid -> valid_mod

Rules:
- Match whole identifiers only (avoid replacing inside longer words).
- Preserve case (if the PDF shows uppercase variants, apply equivalent mapping).

If you prefer a different mapping, tell me and I will update and reapply.
