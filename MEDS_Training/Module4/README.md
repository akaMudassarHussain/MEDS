# Module4 for MEDS Training

This folder contains the Module4 materials prepared from a reference repository. Per your request:

- The DirectMappedCacheController directory has been excluded.
- Original PDF source files are referenced below (not embedded to avoid accidental verbatim copy).
- Small edits to PDFs (variable renames) were requested. Instead of modifying binary PDFs directly, this commit provides:
  - edit_mapping.txt — the exact mapping to apply to variables (automated/script-friendly).
  - a list of original PDF URLs and recommended filenames.
  - renamed filenames suggestions (with _v2 suffix) to show intended edited copies.
- Task1..Task13 folders have been created and include starter README.md and solution.sv placeholder files.

If you want me to perform the actual PDF textual edits and regenerate PDF files, provide the editable source files (DOCX/LaTeX) or confirm that it's OK to attempt extraction/reconstruction from the PDFs (this often alters layout). For a zero-risk workflow I recommend applying the edit mapping to editable sources and then re-exporting PDFs.
